# ============================================================
# 密钥与本地环境 / secrets & local env
# ============================================================
.env
.env.local
.env.*.local
*.pem
*.key
*_secret
*_secret.*

# ============================================================
# Python 缓存 / python cache
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ============================================================
# Phase 2 staging 中转区（不入真源，不入 git）
# ============================================================
_staging/

# KS-SCHEMA-005 §8 artifact：每次跑 validate_serving_tree.py 刷新的 CI 证据，不入 git
scripts/validate_serving_tree.report
# KS-POLICY-005 §8 artifact：每次跑 validate_model_policy.py 刷新的 CI 证据，不入 git
scripts/validate_model_policy.report

# ============================================================
# 编辑器与系统 / editor & OS
# ============================================================
.vscode/
.idea/
.DS_Store
*.swp
*.swo
*~

# ============================================================
# 本地工具元数据 / local tool metadata（不入仓 / not committed）
# ============================================================
.claude/
.codex
.cursor/
# AGENTS.md 保留为追踪文件 / kept tracked（Codex 审查员协议，非密）

# ============================================================
# 日志与临时文件 / logs & temp
# ============================================================
*.log
*.tmp
.coverage
htmlcov/

# ============================================================
# KS-DIFY-ECS-004 audit：dry-run / rollback 走 CI artifact，不入 git
# canonical apply audit 仍入 git（见 task card §8）
# ============================================================
knowledge_serving/audit/qdrant_upload_KS-DIFY-ECS-004.dry_run.json
knowledge_serving/audit/qdrant_upload_KS-DIFY-ECS-004.rollback.json

# KS-CD-002 rollback audit：每次 dry-run / apply 产时间戳文件，CI artifact
knowledge_serving/audit/rollback_KS-CD-002_*.json

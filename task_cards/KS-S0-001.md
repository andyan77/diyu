---
task_id: KS-S0-001
phase: S0
wave: W0
depends_on: []
files_touched:
  - clean_output/scripts/w12_*.py
artifacts:
  - clean_output/audit/baseline_alignment_KS-S0-001.md
s_gates: [S0]
plan_sections:
  - "§12 S0"
  - "§A2.1"
writes_clean_output: true
ci_commands:
  - python3 clean_output/scripts/full_audit.py
status: done
---

# KS-S0-001 · 修 W12-T13 基线漂移

## 1. 任务目标
- **业务**：让 27 道硬门基线重新可信，否则下游所有 serving 卡都建立在错基线上。
- **工程**：把 W12 对抗测试期望值从 `06_rule=194 / 07_evidence=194` 更新为当前真源行数（实测后填入）。
- **S gate**：S0 baseline_alignment（§12）。
- **非目标**：不改 9 表 CSV、不重算证据、不引新规则。只改测试常量。

## 2. 前置依赖
- 仓库可执行 `clean_output/scripts/full_audit.py`
- 无其他卡前置（DAG 起点）

## 3. 输入契约
- 读：`clean_output/nine_tables/06_rule.csv`, `07_evidence.csv`
- 读：W12 测试脚本 `clean_output/scripts/w12_*.py`
- 不读：任何外部服务、任何旧工作区产物

## 4. 执行步骤
1. `wc -l clean_output/nine_tables/06_rule.csv clean_output/nine_tables/07_evidence.csv` → 取 N6 / N7
2. `grep -rn "194" clean_output/scripts/ | grep -E "(w12|rule|evidence)"` 定位旧期望值
3. 把 `194` 替换为 N6 / N7，加注释 `# baseline aligned to manifest YYYY-MM-DD`
4. 重跑 `python3 clean_output/scripts/full_audit.py`
5. 把 N6 / N7 / manifest hash 写入 `clean_output/audit/baseline_alignment_KS-S0-001.md`

## 5. 执行交付
| 路径 | 格式 | canonical | 可重建 | 入 git | CI artifact |
|---|---|---|---|---|---|
| `clean_output/scripts/w12_*.py`（修改） | py | 否 | 是 | 是 | 否 |
| `clean_output/audit/baseline_alignment_KS-S0-001.md` | md | 是 | 否（一次性证据） | 是 | 是 |

## 6. 对抗性 / 边缘性测试
| 测试 | 命令 | 期望 |
|---|---|---|
| 期望值漏改 | `grep -rn "194" clean_output/scripts/` | 0 命中 |
| 9 表行数与脚本常量背离 | 校验脚本 | 一致 |
| 重跑幂等 | 连跑两次 full_audit | 两次 26 pass + 1 deprecated_pass（共 27 total · 0 fail） |
| 空 CSV | mv 后跑 | audit FAIL 不静默 |
| 重复 ID | `awk ... uniq -d` | 空 |

## 7. 治理语义一致性测试
- clean_output 仍为真源：**只**改测试常量，9 表 0 改动（`git diff --stat clean_output/nine_tables/` == 0）
- knowledge_serving 不存在 / 不被引用
- 不引入 tenant / brand 判断
- 不调 LLM
- compile_run_id / source_manifest_hash 留给 KS-S0-006

## 8. CI 门禁测试
```
command: python3 clean_output/scripts/full_audit.py
pass: exit 0 + 汇总满足 fail=0 且 (pass + deprecated_pass) == total
      （Phase 2 2026-05-12 起 G6 load_to_sqlite 已 deprecated；当前基线为 26 pass + 1 deprecated_pass + 0 fail · total 27）
failure_means: 基线未对齐，禁止任何 serving 卡开工
artifact: clean_output/audit/audit_status.json + final_report.md
```

## 9. CD / 环境验证
不涉及部署。仅本地 + CI。无 secrets。

## 10. 独立审查员 Prompt
> 第三方审查员，**不得用 LLM 主观判断作为通过依据**。请：
> 1. 读 `task_cards/KS-S0-001.md` 与 PR diff
> 2. `git diff main -- clean_output/nine_tables/` 必须为空
> 3. `wc -l` 当前 06_rule / 07_evidence 行数与脚本新常量必须一致
> 4. `python3 clean_output/scripts/full_audit.py` 必须 exit 0 + 汇总 fail=0 且 (pass + deprecated_pass) == total（当前基线 26 pass + 1 deprecated + 0 fail）
> 5. 抽查 `baseline_alignment_KS-S0-001.md` 含 manifest hash + 日期
> 6. 输出 pass / conditional_pass / fail，列阻断项
> 阻断项：9 表被改；任一硬门 fail；新常量与 CSV 实际行数不符。

## 11. DoD
- [ ] CI full_audit 通过：exit 0 + fail=0 + (pass + deprecated_pass) == total（基线：26 + 1 = 27）
- [ ] 9 表 CSV `git diff --stat` == 0
- [ ] `baseline_alignment_KS-S0-001.md` 落盘
- [ ] 无密钥入仓
- [ ] 审查员 pass

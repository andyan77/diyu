---
task_id: KS-S0-002
phase: S0
depends_on: [KS-S0-001]
files_touched:
  - clean_output/storage/single_db_logical_isolation.sql
  - clean_output/audit/db_state_evidence_KS-S0-002.md
artifacts:
  - clean_output/audit/db_state_evidence_KS-S0-002.md
s_gates: [S0]
plan_sections:
  - "§12 S0"
  - "§A2.2"
writes_clean_output: true
ci_commands:
  - python3 scripts/verify_db_vs_csv.py --strict
status: done
---

# KS-S0-002 · knowledge.db（旧物理库 / legacy sqlite db）状态快照与处置

## 1. 任务目标
- **业务**：消除"旧物理库 / legacy DB 可能被当成隐含真源 / implicit source of truth"的风险。
- **工程**：执行二选一处置 + 落盘**事实快照 / state evidence**（不是 ADR / 架构决策记录）——(A) 重建 `knowledge.db` 并与 9 表 CSV / manifest（清单 hash）一致；或 (B) 显式废弃 / deprecate 并从 serving 信任链移除。
- **S gate**：S0。
- **非目标**：不引入新的存储引擎 / storage engine；不改 9 表数据；**不产生 ADR**——本卡产出物是当前状态的客观快照 + 处置记录，不是治理决策文档。

## 2. 前置依赖
- KS-S0-001 完成（基线对齐）

## 3. 输入契约
- 读：`clean_output/nine_tables/*.csv`、当前 `knowledge.db`（若存在）、`clean_output/audit/source_manifest.json`
- 不读：任何 ECS / Qdrant

## 4. 执行步骤
1. 处置选择：A 重建 / B 废弃。事实快照写入 `clean_output/audit/db_state_evidence_KS-S0-002.md`，含：当前 DB 文件 sha256、与 CSV 行数差异、选定路径（A 或 B）、回滚命令；**不写**架构动机 / 长期演进等 ADR 内容
2. (A 路径) 写 `clean_output/storage/single_db_logical_isolation.sql` 重建脚本，跑后 `SELECT count(*) FROM ...` 与 CSV 行数一致；含 `brand_layer`（品牌层）列
3. (B 路径) 删 / 改名 `knowledge.db` → `knowledge.db.deprecated_<date>`；grep 全仓无 serving 脚本引用它
4. 跑 `scripts/verify_db_vs_csv.py --strict`：A 路径要求一致；B 路径要求无引用

## 5. 执行交付
| 路径 | 格式 | canonical | 可重建 | 入 git | CI artifact |
|---|---|---|---|---|---|
| `clean_output/storage/single_db_logical_isolation.sql`（A） | sql | 是 | 是 | 是 | 否 |
| `clean_output/audit/db_state_evidence_KS-S0-002.md` | md | 是 | 否 | 是 | 是 |

## 6. 对抗性 / 边缘性测试
| 测试 | 期望 |
|---|---|
| DB 与 CSV 行数不一致（A 路径） | verify 脚本 exit ≠ 0 |
| B 路径下仍有脚本引用旧 DB | grep 命中即 fail |
| 重建 SQL 缺 brand_layer 列 | schema check fail |
| 跨租户 SQL 注入测试样例 | 仍按 brand_layer 隔离 |
| 重跑 SQL 不幂等 | 视为 fail |

## 7. 治理语义一致性
- 处置选择必须显式（不允许"暧昧保留 / ambiguous keep"）
- 产出形态是**事实快照 + 运行证据 / state snapshot + runtime evidence**，不是 ADR / 架构决策记录
- 若 B 路径：`knowledge_serving/` 与 `task_cards/` 下任何脚本 grep `knowledge.db` 必须 0 命中
- 若 A 路径：DB 行 `brand_layer ∈ {domain_general, brand_<name>, needs_review}`
- 不调 LLM

## 8. CI 门禁
```
command: python3 scripts/verify_db_vs_csv.py --strict
pass: 路径 A 行数全一致；路径 B 0 引用
failure_means: 旧 DB 仍可能被误用为真源
artifact: clean_output/audit/db_state_evidence_KS-S0-002.md
```

## 9. CD / 环境验证
不部署。secrets 检查：DB 文件不含明文密码（A 路径）。

## 10. 独立审查员 Prompt
> 第三方审查员，禁止用 LLM 判断替代命令。请：
> 1. 读卡 + diff
> 2. 确认 `db_state_evidence_KS-S0-002.md` 给出 A/B 明确选择 + DB 文件 sha256 + 与 CSV 行数差异 + 回滚命令；不应含 ADR-类长期决策叙事
> 3. A 路径：跑 verify_db_vs_csv，所有表行数一致
> 4. B 路径：`grep -rn "knowledge.db" --include="*.py" --include="*.yaml"` 在 knowledge_serving/ 与 task_cards/ 下 0 命中
> 5. 输出 pass / conditional_pass / fail
> 阻断项：决策不明确；A 路径行数差异；B 路径仍有引用。

## 11. DoD
- [ ] 事实快照文档 / state evidence 落盘（非 ADR）
- [ ] verify 命令 pass
- [ ] 9 表 CSV 0 改动
- [ ] 审查员 pass

---
task_id: KS-S0-004
phase: S0
wave: W0
depends_on: [KS-S0-003]
files_touched:
  - scripts/check_qdrant_health.py
  - knowledge_serving/policies/qdrant_fallback.yaml
artifacts:
  - scripts/check_qdrant_health.py
  - clean_output/audit/qdrant_health_KS-S0-004.json
s_gates: [S0]
plan_sections:
  - "§A2.5"
  - "§8"
writes_clean_output: true
ci_commands:
  - python3 scripts/check_qdrant_health.py --strict --env staging
status: done
---

# KS-S0-004 · Qdrant 健康检查 + structured-only fallback 开关

## 1. 任务目标
- **业务**：保证 Qdrant 不可用时 serving 不全挂；让健康状态成为流水线显式信号。
- **工程**：实现 `check_qdrant_health.py` + fallback 策略 yaml。
- **S gate**：S0。
- **非目标**：不灌库（属 KS-DIFY-ECS-004）；不实现召回（属 KS-RETRIEVAL-006）。

## 2. 前置依赖
- KS-S0-003（env 注入就绪）

## 3. 输入契约
- 读：env `QDRANT_URL_STAGING`, `QDRANT_API_KEY`
- 不读：prod endpoint

## 4. 执行步骤
1. 写 `scripts/check_qdrant_health.py`：调用 `/healthz` 或 `/collections`，返回 ok/down + 延迟
2. `--strict` 模式：down 即 exit 1；非 strict：down 返回 fallback 信号
3. 写 `knowledge_serving/policies/qdrant_fallback.yaml`：`when: down → strategy: structured_only`；声明 timeout、retry、降级期望
4. 健康检查结果落 `clean_output/audit/qdrant_health_KS-S0-004.json`

## 5. 执行交付
| 路径 | 格式 | canonical | 可重建 | 入 git | CI artifact |
|---|---|---|---|---|---|
| `scripts/check_qdrant_health.py` | py | 是 | — | 是 | — |
| `knowledge_serving/policies/qdrant_fallback.yaml` | yaml | 是 | — | 是 | — |
| `clean_output/audit/qdrant_health_KS-S0-004.json` | json | 是（运行证据） | 是 | 是 | 是 |

## 6. 对抗性 / 边缘性测试
| 测试 | 期望 |
|---|---|
| Qdrant 关闭 | --strict exit 1；非 strict 返回 fallback |
| 延迟 > 阈值 | 标 degraded |
| API key 错 | exit 1 + 明确错误 |
| prod endpoint 误用 | 拒绝执行 |
| 重跑幂等 | 是 |

## 7. 治理语义一致性
- 不读 prod
- 不调 LLM
- fallback 决策落盘 yaml，不交给运行时随便走

## 8. CI 门禁
```
command: python3 scripts/check_qdrant_health.py --strict --env staging
pass: exit 0 或在 down 情况下 fallback yaml 存在且有效
failure_means: 召回链路不可信
artifact: clean_output/audit/qdrant_health_KS-S0-004.json
```

## 9. CD / 环境验证
- staging 探活；prod 探活仅在 KS-CD-001 中按需触发
- 监控：Qdrant up/down 指标，degraded 告警
- secrets：env 注入，无硬编码

## 10. 独立审查员 Prompt
> 请：
> 1. 跑 `check_qdrant_health.py --env staging`
> 2. 模拟 Qdrant down（断网或假 URL），确认 --strict exit 1
> 3. 检查 fallback yaml 存在且 schema 正确
> 4. 尝试 `--env prod`，必须拒绝
> 5. 输出 pass / conditional_pass / fail
> 阻断项：down 时静默 pass；prod endpoint 未拒绝；fallback yaml 缺失。

## 11. DoD
- [ ] 健康检查脚本入 git
- [ ] fallback yaml 落盘
- [ ] CI 命令 pass
- [ ] 审查员 pass
